{
  "name": "node-google-admin-sdk-test",
  "version": "1.0.0",
  "description": "Test repository to validate scope declaration, setup of GCP service account and API Controls Domain-wide Delegation with Google Admin SDK",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node -r dotenv/config build/index"
  },
  "repository": "https://github.com/yo0z/node-google-admin-sdk-test",
  "author": "Yo0z",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "googleapis": "^59.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.6.0",
    "typescript": "^4.0.2"
  }
}
